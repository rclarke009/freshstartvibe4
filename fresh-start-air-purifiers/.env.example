# The variables added in this file are only available locally in MiniOxygen.
# Run `h2 link` to also inject environment variables from your storefront,
# or `h2 env pull` to populate this file.

# Session Security
SESSION_SECRET="your_session_secret_here"

# Shopify Storefront API Configuration
# IMPORTANT: Use a STOREFRONT API token (starts with shpca_ or shpcs_), NOT an Admin API token (shpat_)
PUBLIC_STOREFRONT_API_TOKEN="your_storefront_api_token_here"
PUBLIC_STORE_DOMAIN="rfbur0-m1.myshopify.com"
PUBLIC_STOREFRONT_ID="your_storefront_id_here"

# Optional: Storefront API Version
# PUBLIC_STOREFRONT_API_VERSION=2025-01  # Matches Hydrogen 2025.1.4 compatibility
